# Project Setup

Build a full-stack car dealership management system. I need a frontend and backend for managing vehicles, customers, sales, and dealership inventory. Create a clean and professional UI and make sure the application is functional end-to-end.

# Frontend UI

Create the frontend with a modern car dealership dashboard design. I want a sidebar, top navigation bar, dashboard cards, tables, buttons, forms, and proper spacing. Make the UI look professional and consistent across all pages.

# Match Reference Screenshots

I want the application UI to closely match the screenshots I provided. Use the same overall layout, spacing, typography, card style, navigation structure, colors, buttons, and visual hierarchy. Do not create a completely different design.

# Dashboard

Create the main dashboard page. Add summary cards for vehicles, customers, sales, and available inventory. Add useful dealership information and make the dashboard visually clean and professional.

# Vehicle Management

Add a vehicle inventory page. Users should be able to view vehicles, add a new vehicle, edit vehicle details, delete a vehicle, and see whether a vehicle is available or sold.

# Customer Management

Create a customer management page. Allow users to add customers, view customer information, edit customer details, and delete customers. Keep the design consistent with the existing UI.

# Backend

Create the backend for the application. Add API endpoints for vehicle and customer management and connect the backend with the frontend. Make sure the API operations work correctly.

# Fix UI Issues

The current application is not displaying the UI correctly. Check the frontend code and fix the layout, CSS, spacing, sizing, navigation, and component issues without removing the existing functionality.

# Fix Errors

Run through the application and identify any errors that prevent the pages or features from working. Fix the errors and make sure the frontend and backend communicate correctly.

# Improve CSS

The functionality is working, but the UI does not look polished enough. Improve the CSS. Fix the spacing, alignment, fonts, buttons, cards, tables, sidebar, and responsive layout while keeping the existing design direction.

# Login Page

Create a professional login page. Match the login page with the overall application design and make sure the page is responsive and visually consistent.

# Settings Page

Create a settings page with a clean professional layout. Keep the same sidebar, header, typography, colors, spacing, and styling used throughout the application.

# Testing

Test the complete application. Check the major frontend pages, forms, navigation, vehicle management, customer management, backend API operations, and error handling. Identify and fix any issues you find.

# GitHub Submission

Prepare the project for GitHub submission. Organize the project into separate frontend and backend folders and keep the application screenshots and documentation organized. Make sure unnecessary generated files are not included in the repository.

# Final Review

Do a final review of the project. Check that the frontend, backend, UI, API functionality, navigation, forms, and major user flows are working correctly. Fix any remaining issues and make the project ready for submission.
